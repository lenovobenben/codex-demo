# offline-checker

一个超小的本地 Python CLI 工具，用于演示：
- 不访问外网
- 从仓库本地路径安装
- 安装后执行命令验证

## 安装（本地路径）

```bash
python -m pip install --no-build-isolation ./tools/offline-checker
```

## 运行

```bash
offline-check --name demo
```
